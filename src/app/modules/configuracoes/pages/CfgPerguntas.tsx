import { HelpCircle } from 'lucide-react';
import { ConfigPage } from './ConfigPage';

export function CfgPerguntas() {
  return <ConfigPage title="Perguntas" description="Banco de perguntas para formulários" icon={HelpCircle} color="#a78bfa" />;
}
